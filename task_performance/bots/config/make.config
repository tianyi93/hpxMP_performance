#Automatically generated by configure
#compilers and linkers

#config name
LABEL=clang


#compilers
CC=clang

#OMPC=clang -fopenmp  -emit-llvm -S -c 
#OMPLINK=clang -fopenmp -emit-llvm -S -c
#CLINK=clang -emit-llvm -S -c

OMPC=clang -fopenmp
OMPLINK=clang -fopenmp
CLINK=clang

#compiler and linker flags

OPT_FLAGS=-O0 

CC_FLAGS=
OMPC_FLAGS=

CLINK_FLAGS=
OMPLINK_FLAGS=
